import angular from 'angular';

export const INSTALL_BUTTON_MODULE_NAME ='installButton';

export const installButtonModule = angular.module(INSTALL_BUTTON_MODULE_NAME, []);

export default installButtonModule;